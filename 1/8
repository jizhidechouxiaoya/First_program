今日学习:
论文向后看了一段，一直在琢磨作者提出的这个排列不变性是指什么意思，我觉得应该这样理解:由于神经网络的每一层节点的数值都需要前一层的权值和上一层节点的数值才能
决定，因此即使知道两个神经网咯的相关参数，也不能确定这两个神经网络结构上是否相等，但如果每个神经网络的权值与节点的排列都能按照一定的规则进行排列，那么就能
知道两个神经网络结构是否相同，这样就可以减少不必要的计算量。
晚上用mininet创建简单的拓扑，一直报错，我想会不会是floodlight控制器和mininet需要分别安装在两台虚拟机上，明天试试
