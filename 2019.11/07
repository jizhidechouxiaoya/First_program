今日学习：
今天上午学习了django创建页面并从页面上选取数据以执行相应操作，下午通过修改LSTM代码的一些参数，解决了损失函数报NAN的现象，但是实验结果又出现了损失函数随
训练次数的增加不断变大的现象，调整了学习率的取值，但仍未解决该问题。通过阅读代码以及结合网上所给出的公式推导，确定自己的反向随机梯度更新未出错，但是不
明白为何是会出现这种现象。打算明天再把pytorch内置的优化器和自身所写的反向传播算法到底有何不同，为何使用优化器就可以正确训练出有效模型，使用自己的就出问题。
